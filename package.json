{
  "name": "teencoin",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",  // Para back-end, se necessário
    "build": "react-scripts build"  // Para criar o build do front-end
  },
  "dependencies": {
    "react": "^17.0.0",
    "react-dom": "^17.0.0"
  }
}
